// vec.c
#include "vec.h"

Vec3 add(Vec3 a, Vec3 b) {
  Vec3 c = {
    .x = a.x + b.x,
    .y = a.y + b.y,
    .z = a.z + b.z,
  };

  return out;
}

Vec3 sub(Vec3 a, Vec3 b) {
  Vec3 c = {
    .x = a.x - b.x,
    .y = a.y - b.y,
    .z = a.z - b.z,
  };

  return out;
}
